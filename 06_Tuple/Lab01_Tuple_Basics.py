'''Tuple ()
- Tuple is one of the COLLECTION & it saves values of same or different DT
- Tuple stores the item in sequence & thus it is ORDERED & follows INDEXATION
- One CAN NOT add, remove, append, pop values at specific index thus tuple is "IMMUTABLE"
- List accepts DUPLICATE values

Tuple has all other properties same as List except Tuple is "IMMUTABLE" - i.e.can not be alter / modify '''

demo_tuple1 = (1,2,3,4,5,6)
demo_tuple2 = ("Pune", "Mumbai", "Nashik", "Kolhapur", "Satara")

# type method
print(type(demo_tuple1))

# Printing values from tuple
print(demo_tuple2[4])
